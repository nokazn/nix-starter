# nix-starter

## Usage

```sh
$ nix flake init --template github:nokazn/nix-starter#rust

# or

$ nix flake new --template github:nokazn/nix-starter ./my-project

# Show a list of all templates
$ nix flake show github:nokazn/nix-starter
```
